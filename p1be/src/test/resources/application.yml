spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: "jdbc:h2:mem:test;MODE=PostgreSQL;DB_CLOSE_ON_EXIT=FALSE;DATABASE_TO_LOWER=TRUE"
    username: "test"
    password: "test"

  jpa:
    show-sql: true

app:
  archivization:
    interval-ms: 1000
    report-age-threshold: PT3H